import { createTheme } from '@mui/material/styles'

export const theme = createTheme({
  palette: {
    type: 'light',
    primary: {
      main: '#951c2a',
    },
    secondary: {
      main: '#1c9587',
    },
  },
  typography: {
    fontFamily: 'Montserrat',
  },
});